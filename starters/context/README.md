# Context

Your long-term memory. Things you've decided are worth keeping.

The agent reads this folder when something it's working on calls for it — or when you ask. Topics get added as patterns emerge from `inbox/scratch.md`.

Right now: empty. That's normal at the start. It fills as you and the agent decide what's worth elevating from scratch into here.

When a subfolder appears in here (like `context/people/`), it means something genuinely warranted its own space — not because we organized prophylactically.
